<Window x:Class="StudyPortal.CourseStudentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudyPortal"
        mc:Ignorable="d"
        Title="Студенты курса" Height="480" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#FAFAFA"
        Loaded="CourseStudentsWindow_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Direction="270"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Курс:" VerticalAlignment="Center" FontWeight="Medium" FontSize="14"/>
                <ComboBox Name="CourseFilterComboBox" Width="350" Margin="12,0,0,0"
                          DisplayMemberPath="course_name"
                          SelectedValuePath="course_id"
                          SelectionChanged="CourseFilterComboBox_SelectionChanged" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="5">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Direction="270"/>
            </Border.Effect>
            <DataGrid Name="CourseStudentsDataGrid" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding student_id}" Header="ID" Visibility="Collapsed" />
                    <DataGridTextColumn Binding="{Binding first_name}" Header="Имя" Width="180" />
                    <DataGridTextColumn Binding="{Binding last_name}" Header="Фамилия" Width="180" />
                    <DataGridTextColumn Binding="{Binding email}" Header="Email" Width="250" />
                    <DataGridTextColumn Binding="{Binding course_name}" Header="Курс" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
