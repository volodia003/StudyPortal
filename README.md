# StudyPortal

Educational portal sample built with WPF and PostgreSQL.

## Quick Start
1. Create database `EduPortal` in PostgreSQL.
2. Run `Database/EduPortal.sql`.
3. Update connection string in `App.config`.
4. Install Npgsql via NuGet.
5. Build and run.

See `REPORT.md` for full details.